# v1 fixture paired with a stray ~/.mp/oauth/tokens_us.json from a half-completed
# login flow (the converter creates a synthetic `oauth-us` account per Research R1).
# The config itself has only one v1 account.

default = "demo-sa"

[accounts.demo-sa]
username = "user.x.mp-service-account"
secret = "demo-secret"
project_id = "3713224"
region = "us"
