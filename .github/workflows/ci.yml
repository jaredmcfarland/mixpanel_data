name: CI

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "tests/**"
      - "mp_mcp/**"
  pull_request:
    branches:
      - main
    paths:
      - "src/**"
      - "tests/**"
      - "mp_mcp/**"

jobs:
  test:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Sync dependencies
        run: uv sync --all-extras

      - name: Lint with ruff
        run: uv run ruff check src/ tests/

      - name: Type check with mypy
        run: uv run mypy src/ tests/

      - name: Run tests with pytest
        if: matrix.python-version != '3.12'
        run: uv run pytest
        env:
          HYPOTHESIS_PROFILE: ci

      - name: Run tests with coverage (Python 3.12 only)
        if: matrix.python-version == '3.12'
        run: uv run pytest --cov=src/mixpanel_data --cov-report=term-missing --cov-report=xml --cov-fail-under=90
        env:
          HYPOTHESIS_PROFILE: ci

      - name: Upload coverage to codecov
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v4
        with:
          fail_ci_if_error: false

      - name: Build package
        if: matrix.python-version == '3.12'
        run: uv build

  test-mcp-server:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Sync MCP server dependencies
        run: cd mp_mcp && uv sync --all-extras

      - name: Lint MCP server with ruff
        run: cd mp_mcp && uv run ruff check src/ tests/

      - name: Type check MCP server with mypy
        run: cd mp_mcp && uv run mypy src/ tests/

      - name: Run MCP server tests with pytest
        if: matrix.python-version != '3.12'
        run: cd mp_mcp && uv run pytest

      - name: Run MCP server tests with coverage (Python 3.12 only)
        if: matrix.python-version == '3.12'
        run: cd mp_mcp && uv run pytest --cov=src/mp_mcp --cov-report=term-missing --cov-fail-under=90

      - name: Build MCP server package
        if: matrix.python-version == '3.12'
        run: cd mp_mcp && uv build
